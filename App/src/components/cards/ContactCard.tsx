import {StyleSheet, Text, View} from 'react-native';
const ContactCard = () => {
  return (
    <View>
      <Text style={{color: '#000'}}>ContactCard</Text>
    </View>
  );
};

export default ContactCard;

const styles = StyleSheet.create({});
